{
  "name": "drawthings-web",
  "version": "1.0.0",
  "description": "Mobile-friendly web interface for Draw Things API",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "server": "cd server && python -m uvicorn main:app --reload --port 8000"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
